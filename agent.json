{
  "name": "set_status",
  "description": "Updates the public site status via POST",
  "method": "POST",
  "endpoint": "/api/set_status",
  "parameters": {
    "status": "Online or Offline"
  }
}
